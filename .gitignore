# Hermes / Codex internal state
.agents/
.codex/

# MLflow runtime (re-generatable)
mlflow.db
mlflow.db-journal
mlruns/
mlruns_legacy/

# Cycle workspace outputs (re-generatable from pipelines/)
workspace/cycle_*/

# Generated reports
reports/latest/
reports/cycle_*/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# Editor / OS
.DS_Store
*.swp
*.swo
.vscode/
.idea/

# Windows ADS (WSL)
*:Zone.Identifier

# Secrets — never commit
.env
.env.*
!.env.example

# Large source datasets — AI Hub에서 재다운로드 가능 (수GB 단위)
dataset/1.Training/
dataset/2.Validation/

# 5K stratified subsample (43M, deterministic from extract_5k.py seed=42)
dataset/sample_5k/

# Phase 1 toy sample (342건) + 데이터 설명서 PDF는 추적 유지
# (dataset/sample/, dataset/*.pdf는 ignore 대상 아님)
.gstack/
